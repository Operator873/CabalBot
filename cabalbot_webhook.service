[Unit]
Description = A webhook listener for CabalBot
After = network.target

[Service]
Type = Simple
ExecStart = /usr/bin/python3 /path/to/cabalbot_webhook.py
Restart = on-failure
RestartSec = 5
TimeoutStartSec = infinity

[Install]
WantedBy = multi-user.target